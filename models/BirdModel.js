export default class BirdModel {
    constructor(title, imageURL, category, author) {
        this.title = title;
        this.imageURL = imageURL;
        this.category = category;
        this.author = author;
    }
}